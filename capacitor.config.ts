import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cinestreambyadil.app',
  appName: 'CineStream By Adil',
  webDir: 'dist'
};

export default config;
